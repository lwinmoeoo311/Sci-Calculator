package Operation;

public class Subtraction extends Operator{

    public Subtraction(String opt) {
        super(opt);
    }

    @Override
    public <T> T calculate(T num) {
        return super.calculate(num);
    }
}
